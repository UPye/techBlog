<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Link for Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <link rel="shortcut icon" href="/blogIcon.png" type="png">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>

<body>
    <div class="wrapper">
    
    <!-- Display header and contain Nav and Logo -->
    <header class="bg-info">
        <nav class="nav navbar-light bg-info ms-4 fs-4 ">
            <a class="nav-link link-light" href="/">Home</a>
            {{#if loggedIn}}
            <a class="nav-link link-light" href="/dashboard">Dashboard</a>
            {{/if}}
            {{#unless loggedIn}}
            <a class="nav-link link-light" href="/login">Login</a>
            <a class="nav-link link-light" href="/signup">Sign Up</a>
            {{/unless}}
            {{#if loggedIn}}
            <a id="logout" class="nav-link link-light" href="">Logout</a>
            {{/if}}
        </nav>
        <!-- Display Logo -->
        <div class="bg-info mb-5">
            <button type="button"
                class="btn btn-primary text-white border border-primary border-2 rounded-5 ms-5 my-3 fs-1 px-3 text-center">Tech
                Blog</button>
        </div>
    </header>
    <section>
        {{{body}}}
    </section>
</body>
{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}

</html>